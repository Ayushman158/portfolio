export const metadata = {
  title: 'Motion studies — Kizuku’s gestures, playable',
  description:
    'Three interactions from the Kizuku iOS app rebuilt for the browser: a damped drag with velocity release, a hold that can be abandoned, and a growth sequence. Same constants, same release logic as the shipped React Native code.',
};

export default function MotionLayout({ children }) {
  return children;
}
