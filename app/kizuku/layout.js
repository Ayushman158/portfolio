export const metadata = {
  title: 'Kizuku — a wellness app for people who overthink the future',
  description: 'Turning one worry into one grounded action a day. Research, brand, design system and the iOS build — with the interactions playable.',
};

// The case study is written in the site's voice, not the product's brand font,
// so Satoshi is no longer loaded here. It is described in the system section
// instead, which is where it belongs.
export default function KizukuLayout({ children }) {
  return children;
}
